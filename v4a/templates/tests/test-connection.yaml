apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "v4a.fullname" . }}-test-connection"
  labels:
    {{- include "v4a.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "v4a.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
